{
  "name": "coa-schemas",
  "version": "0.0.2-3",
  "description": "Certificates of Analysis (CoA) as defined by EN 10204, developed by S1Seven",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/s1seven/CoA-schemas.git"
  },
  "author": "",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/s1seven/CoA-schemas/issues"
  },
  "homepage": "https://github.com/s1seven/CoA-schemas#readme",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./*.js",
    "lint:staged": "lint-staged",
    "format": "prettier --write \"**/*.{js,json}\"",
    "test": "jest --runInBand",
    "update-version": "node ./update-version",
    "release": "read -p 'GITHUB_TOKEN: ' GITHUB_TOKEN && export GITHUB_TOKEN=$GITHUB_TOKEN && release-it"
  },
  "devDependencies": {
    "@s1seven/schema-tools-generate-html": "^0.0.20",
    "@s1seven/schema-tools-utils": "^0.0.17",
    "ajv": "^8.6.0",
    "ajv-formats": "^2.1.0",
    "eslint": "^7.30.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "html-differ": "^1.4.0",
    "jest": "^27.0.6",
    "lint-staged": "^11.0.1",
    "pre-commit": "^1.2.2",
    "prettier": "2.3.2",
    "release-it": "*"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint"
    ]
  },
  "pre-commit": [
    "format",
    "lint:staged"
  ],
  "release-it": {
    "github": {
      "release": true
    },
    "npm": {
      "publish": false
    },
    "hooks": {
      "before:init": [
        "npm run lint",
        "npm run test"
      ],
      "after:bump": "npm run update-version v${version} ${name}",
      "after:release": "echo Successfully released ${name} v${version} to ${repo.repository}."
    }
  },
  "jest": {
    "rootDir": ".",
    "maxConcurrency": 2,
    "testRegex": ".*\\.spec\\.js$",
    "moduleFileExtensions": [
      "js",
      "json",
      "node"
    ]
  }
}
